{
  "name": "payment-webhook-processor",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "prisma:generate": "prisma generate",
    "package": "npm run prisma:generate && npm run build"
  },
  "dependencies": {
    "@prisma/client": "^5.7.1",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.119",
    "prisma": "^5.7.1",
    "typescript": "^5.3.3"
  }
}
