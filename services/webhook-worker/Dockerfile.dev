FROM node:20-alpine

WORKDIR /app

# Copiar arquivos de dependências
COPY package.json package-lock.json* ./
RUN npm ci

# Copiar código fonte
COPY . .

# Copiar schema Prisma da API
COPY ../api/prisma ./prisma

# Gerar Prisma Client
RUN npm run prisma:generate

CMD ["npm", "run", "dev"]
